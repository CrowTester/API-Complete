-- create table if not exists filmes (
--   id serial primary key,
--   titulo text not null unique,
--   genero varchar not null,
--   imagem varchar not null unique
-- );

-- select titulo , caminho_imagem from armazena_imagem;
-- drop table filmes;

-- select titulo, genero, imagem from filmes;